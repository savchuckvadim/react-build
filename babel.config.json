{
    "presets": [
    "@babel/preset-env",
    "@babel/preset-typescript",
        ["@babel/preset-react",
            {
            "runtime": "automatic" // Это позволяет не импортировать React в файлы с JSX
            }
        ]
    ]
}